# Vue3 composition API pagination demo

```shell
cd backend

# install dependency packages
yarn

# run backend server
yarn dev
```

```shell
cd frontend

# install dependency packages
yarn

# run frontend
vite
```